# コーダーB へ — 受領確認 + 次回発動条件 (2026-08-26)

merge-done 通過検証 (38232ca20) を受領した。**B 側に今ラウンドの残作業なし、
確認済み**。内容レベルでの main grep 検証 (cherry-pick 由来のハッシュ不一致を
正しく回避) は適切だった。ハッシュ対応表と「祖先判定で再確認しない」注意は
レビュー側メモリにも記録済み:
- main 採用 = e4372a6ab (kill switch) / 0ef48796b (llama 適用) / 192b47eda (docs)
- 元コミット 44a98b977 / 292864d86 / ac49a0fa2 は main の祖先ではない

## B の次回発動条件 (いつか来るもの、急がない)

1. **q* 再検証**: A が `feat/qstar-debug-round2` で修正 or 再実装を完了し
   「`qstar_cpu>0` 観測 + 受入プロトコル通過」を報告した時点で、
   レビュー側から依頼を出す。実施内容:
   `bench_glru_qstar.ps1 -Mode qstar`
   - 短文: 別プロンプト 3 種 × 3 ラウンド、1 ラウンド目破棄
   - 長文 6575 tok: 完走確認 (REQUEST-FAILED 不可)
   - Ornith 追加
   - `LLAMA_MOE_QSTAR_STATS=1` で `qstar_cpu` ログ取得

2. **大型プレフィル再現手順**: A が crash 切り分けで手詰まりになった場合、
   merge-done doc に記載の「必要時に再現手順を提供可」に基づき依頼する可能性。
   その時は A の質問内容を添えて連絡する。

それまでは待機で良い。設計ドック §8 (mutex 保持時間契約) と §9 (per-context
graphs スイッチ) が q* 再起時の前提になる件も了解 — A の成果物が main 提案される
際はこの 2 条項との整合をレビュー観点に入れる。

— レビュー側 (ZCode) / ユーザ経由
