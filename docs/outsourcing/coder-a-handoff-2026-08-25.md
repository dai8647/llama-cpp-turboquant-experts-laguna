# A への引き継ぎ依頼 (2026-08-25 夜) — 組織的集約

## 依頼の全体像

ユーザーへ: 以下を A に転送し、回答を A-reply の次の版 (または
`coder-a-progress-large-prefill-2026-08-25.md`) に記録してください。

私が A に直接連絡できないため、ユーザーが中継役です。

## 依頼 1: B 発見の大型プレフィル時 paging クラッシュ (最優先)

詳細は `coder-a-bug-large-prefill-2026-08-25.md` を参照。
A 環境で再現確認 → 切り分け → 修正 → 検証 (私 / B がサポート) をお願いします。

## 依頼 2: q* 二者択一の最終決定

私の監査結果 (検証 21) + A 公式回答を踏まえ、 **main マージ方針の最終決定**
をお願いします。

### 現状整理

| 状態 | 内容 |
|---|---|
| q* 受入数字 (13.43/15.78/13.07) | A 撤回、 warmup バイアス過大評価を認める |
| q* テンソルレイアウト修正 (1e3af73a2+8a0a55bbd) | Huihui layer 0 通過は確認、 ただし calibrate 3回ループで死亡 |
| q* 私の再検証 (84e145355 HEAD) | LOAD 8.6s でサーバ死亡、 A 報告と完全一致 |
| q* 機能完成度 | **未完成**、 ロードすら安定しない |
| グラフ自動無効化 (B 44a98b977+292864d86) | 完成、 検証で劣化なし |
| prefill PF + ページング (B 3b49ca50a+8ca1025a6) | 完成、 検証で OK |
| cache_mutex 契約 (B 設計 §8) | 構造的に健全、 順序デッドロック不可能 |

### 選択肢

| 案 | 内容 | メリット | デメリット |
|---|---|---|---|
| **(a) q* 残して debug+ASan** | A 環境で 30-60 分 debug、 解決後 q* 込みで main マージ | q* 機能を完全には捨てない | 解決まで main マージ保留、 確実な時間コスト |
| **(b) q* revert + B のみ main マージ (私の推奨)** | 991bf3042+1e3af73a2+8a0a55bbd+84e145355 を revert、 B の検証済み機能 (prefill PF + ページング + グラフス自動 disable + 設計 doc + 監査) のみ main へ、 q* は次サイクル | 検証済み機能を main に早く乗せられる、 main 負債リスク低 | q* は一旦 main から外れる、 次サイクルで debug |
| **(c) q* 残し別ブランチで debug** | feat 全体残し、 main には B のみ、 q* debug は feat 継続 | 両方を並行進行 | feat 肥大化、 A の作業環境複雑化 |

### 私の推奨: (b)

理由:
- 「超絶高速化」のメインは実は **B の prefill PF** で、 q* は付録
- main に検証済み機能 (B 部分 + 監査記録) を早く乗せる方が main 負債リスク低い
- q* は feat に残るので A の debug 環境は維持される
- 依頼 1 (大型プレフィル時 paging クラッシュ) 解決後に q* debug を本格化、
  両方解決したタイミングで q* 込みで main へ追加マージ

## 依頼 3 (オプション): B への依頼文も確認

`coder-b-followups-2026-08-25.md` の 2 件は B が回答済 (グラフ API 変更 +
cache_mutex 契約追加)。 追加で B に依頼したいことがあれば別途伝えます。

## 私の現在の体制

- feat HEAD = 84e145355 (push 済)
- メモリ更新済 (A 公式回答 + B 公式回答 + 新規バグ反映)
- main マージ保留中
- q* 再検証ハーネス (`qstar_huihui_r2.ps1` 系) 即起動可能
- B の大型プレフィル時 paging クラッシュ再現テストハーネス (B 設計を踏襲)
  も作成可能、 必要なら依頼してください

## ユーザーへの依頼

1. 上記依頼 1 + 2 を A に転送
2. A の回答 (進捗 + 最終判断) を取得次第、 私に連絡
3. (b) 選択の場合: A 承認後 5 分以内に main マージ完了予定
   (q* 4 commit を revert するブランチ `feat/b-only` を作成)
4. (a) 選択の場合: A の debug 完了後、 私が q* 再検証 → 結果次第で main マージ
