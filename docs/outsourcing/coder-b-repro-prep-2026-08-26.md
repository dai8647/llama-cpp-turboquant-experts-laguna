# コーダーB へ — 待機中タスク 1 件 (2026-08-26)

standby 状態は継続のまま、**GPU を占有しない軽作業を 1 件だけ先出しする**。
A の step 1 (Debug ビルド) と並行しても競合しない範囲のもの。

## タスク: 大型プレフィル + glru クラッシュの再現手順 doc を今書いてほしい

これまでは「A が手詰まった時に提供可」だったが、この順番だと A が詰まってから
手順起票 → 手戻りが発生する。**今のうちに**以下を
`docs/outsourcing/coder-b-large-prefill-repro-2026-08-26.md` として commit/push して
おいてほしい。A がいつでも参照できる状態にする。

### 記載してほしい内容

1. **再現コマンド一式** (コピペで動く形)
   - モデルパス / `--moe-gpu-expert-slot-num 96` / glru 有効化の正確なフラグ
   - `GGML_CUDA_DISABLE_GRAPHS=1` 版と新キルスイッチ版の両方
   - プロンプト投入方法 (6575 tok の入力ファイル名 or 生成方法)
2. **クラッシュシグネチャ**
   - 起動から ~13 秒、layer 18、無言終了 (stderr assertion なし)
   - ロードログのどの行まで進むか (listening 出力前?)
3. **切り分け済み事実** (B が既に確認した範囲の列挙)
   - warmup decode + paging は成功 (= decode 経路の paging は正常)
   - graphs OFF 2 方法どちらでも再現 (= graphs 非依存)
   - slot 数を変えた時の挙動 (slot30 では? 記録があれば)
4. **B が見た疑い箇所**: prefill ubatch 中の `ensure_resident` / `evict` 競合の
   具体的な根拠 (どの観測からそう思ったか)

### 制約

- **GPU 実行しないこと** (A が debug セッション中)。既存セッションの記憶と
  ログからの文書化作業のみ。実機再現が必要になったら A 側 or GPU 空き時に。
- 数字なき完了報告禁止ルールの例外 (文書化タスクのため)。ただし
  「再現手順がコピペ 1 発で動く形か」は自己チェックすること。

## 併せて確認 (回答は任意・短くて良い)

q* 再検証発動時、`bench_glru_qstar.ps1 -Mode qstar` は main HEAD 用に
引数変更が必要か? (q* drop 後の main には `-moe-qstar` フラグが存在しないので、
A の round-2 ブランチのバイナリを使う前提で合っているかの確認のみ)
